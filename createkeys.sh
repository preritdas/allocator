#!/bin/sh

mv 'keys (sample).ini' 'keys.ini'
echo "Created template keys.ini and removed the sample file."
echo "See https://github.com/preritdas/allocator#keysini to understand how exactly to format keys.ini."