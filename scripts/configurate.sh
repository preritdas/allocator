#!/bin/sh

mv 'keys (sample).ini' 'keys.ini'
mv 'config (sample).ini' 'config.ini'
echo "Created template config.ini and keys.ini. Removed the sample files."
echo "See https://github.com/preritdas/allocator#keysini to understand how exactly to format each file."